# Ashot 产品化实施与验证记录

日期：2026-09-12。设计契约：[`07-customer-product-plan.md`](07-customer-product-plan.md)。起始代码：`34d9c3a`。

## 交付顺序

1. 固化设计、回归数据损失场景。
2. 文档事务、无损裁剪/贴图、可撤销编辑、统一导出与 OCR 隔离。
3. 首次使用、按需权限、捕获目标、倒计时、敏感模式和预览恢复。
4. 历史策略与安全迁移、滚动截图及已有辅助能力。
5. 更新集成、发布检查、可访问性与验收证据。

实现、自动化验证、真实系统/GUI 验证和正式发布分别记录，不互相替代。

## 当前状态

| 批次 | 实现 | 验证 |
| --- | --- | --- |
| 设计契约 | 已落盘 | 文档检查 |
| 编辑与导出 | 原图/图层/裁剪文档、分组撤销、统一像素渲染、导出与 OCR 隔离已实现 | 纳入首批 80 项 Swift 测试；真实输入法/窗口交互仍需独立验收 |
| 首次使用与截图 | 首次使用、按需权限、敏感捕获、窗口选择、区域调整、倒计时、重复目标、预览队列已实现 | 纳入首批 80 项 Swift 测试；TCC 系统弹窗/多屏交互未由单测替代 |
| 历史与高级能力 | 待实施 | 待运行 |
| 更新与正式分发 | 待实施 | Developer ID、公证及更新端到端需独立证据 |

## 执行约束

- 不读取/改写 TCC 数据库，不修改 Gatekeeper，不为截图重启 Finder。
- 不覆盖未审核的用户变更，不自动删除既有历史，不给公开源码擅自添加授权许可证。
- 发布凭据仅留在钥匙串；截图不因 GitHub 分发或更新而上传。
- 应用与测试使用相同源码；每批先验证再提交，最终同步原工作目录。
- 原工作目录为 `/Users/ai/project/macOS/Ashot`；沿用上次的另一台 Mac 隔离克隆运行构建，避免阻塞原电脑的其他项目。

## 批次一：编辑、首次使用与截图闭环

- `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer ./dev.sh test`：80 项测试 / 16 个套件全部通过。
- 本机日志：`build/logs/product-core-tests.log`（不上传可能包含机器路径的完整系统日志）。
- 新增非破坏性 `CaptureDocument`，完整操作撤销/重做、图片图层、可撤销美化与统一 `CanvasRenderer`。
- 所有编辑输出、OCR、钉图均使用合成结果；实色遮盖强制不透明；sRGB 像素采样避免 calibrated color 二次转换。
- 预览和编辑器提供 PNG 文件承诺拖拽；导出格式/像素倍数一致，写入失败保留原有编辑，唯一命名使用原子排他改名。
- 新用户默认会话内历史；首次截图主动申请屏幕权限；从系统设置返回不会自动截图；敏感模式不自动复制、自动保存或持久化原图。
- 窗口高亮选择、原区域重截、可取消倒计时，以及不修改 Finder 全局设置的桌面窗口过滤已实现。
- 尚待后续批次：持久历史安全迁移/保留策略、可靠长图捕获、设置与完整翻译、可恢复草稿、更新器与正式发布验收。
